# 🧠 TS Practice Project

Proyecto base para practicar TypeScript, enfocado en:
- Tipado estricto
- Control de flujo
- Manejo de objetos
- Uso de map, filter, find, for
- Type narrowing

## 📦 Instalación
Clonar el repositorio y entrar al proyecto:

```bash
git clone <repo-url>
cd playground-ts
npm install
```

## 🚀 Ejecutar el proyecto

Este proyecto usa ts-node para ejecutar TypeScript directamente sin compilar.

▶ Ejecutar en modo desarrollo

```bash
npm run dev:run
```

## 🏗 Alternativa: Compilar y ejecutar

```bash
npm run build
npm run start
```

## 👀 Modo Watch (recompilación automática)

```bash
npm run dev
```

## 📌 Compilar en watch + ejecutar node en watch (recomendado)

El proyecto esta usando nodemon para vigilar cambios en archivos y reiniciar node, tambien esta usando run all para correr dos scripts al mismo tiempo

```bash
npm run dev:watch
```


